{% extends 'base.html.twig' %}

{% block title %}Modifier{% endblock %}

{% block body %}
    <div class="home-content">
        <div class="container mt-5">
            <h2>Modifier un Santon</h2>

            {{ form_start(form) }}
                <div class="mb-3">
                    {{ form_label(form.nom) }}
                    {{ form_widget(form.nom, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="mb-3">
                    {{ form_label(form.description) }}
                    {{ form_widget(form.description, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="mb-3">
                    {{ form_label(form.imageFile) }}
                    {{ form_widget(form.imageFile, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="mb-3">
                    {{ form_label(form.caracteristique) }}
                    {{ form_widget(form.caracteristique, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="mb-3">
                    {{ form_label(form.valeur) }}
                    {{ form_widget(form.valeur, {'attr': {'class': 'form-control'}}) }}
                </div>
                <button type="submit" class="voir-plus">Mettre à jour</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}

